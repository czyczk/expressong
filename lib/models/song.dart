class Song {
  final int id;
  final String title;
  final String artist;
  final String url;
  final String albumCoverUrl;

  const Song(this.id, this.title, this.artist, this.url, this.albumCoverUrl);
}